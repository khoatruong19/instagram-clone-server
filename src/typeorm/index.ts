import { Follower } from './entities/Follower';
import { Profile } from './entities/Profile';
import { User } from './entities/User';

const entities = [User, Profile, Follower];

export default entities;

export { User, Profile, Follower };
